server {
    listen  80 default_server;
    root /var/www/html;

    server_name {{ domain }} {{ domain_www }};

    if ($scheme != https){
        return 301 https://$server_name$request_uri;
    }
}